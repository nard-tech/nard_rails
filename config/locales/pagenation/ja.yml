# 参考：[Pattern 1] https://github.com/tigrish/kaminari-i18n/blob/master/config/locales/ja.yml

ja:
  helpers:
    page_entries_info:
      #---- Pattern 1
      # more_pages:
      #   display_entries: "<b>%{total}</b>中の%{entry_name}を表示しています <b>%{first} - %{last}</b>"
      # one_page:
      #   display_entries:
      #     one: "<b>%{count}</b>レコード表示中です %{entry_name}"
      #     other: "<b>%{count}</b>レコード表示中です %{entry_name}"
      #     zero: "レコードが見つかりませんでした %{entry_name}"
      #
      #---- Pattern 2
      # more_pages:
      #   display_entries: '%{entry_name}の %{first} - %{last} 件を表示中（合計 %{total} 件）'
      # one_page:
      #   zero: '該当するデータがありません。'
      #   one: '1件中 1-1 件を表示中'
      #   display_entries: '%{entry_name}の 1 - %{count} 件を表示中（合計 %{count} 件）'
      #
      #---- Pattern 3
      more_pages:
        display_entries: '%{first} - %{last} 件を表示中（合計 %{total} 件）'
      one_page:
        zero: '該当データがありません'
        one: '1件中 1-1 件を表示中'
        display_entries: '1 - %{count} 件を表示中（合計 %{count} 件）'

  views:
    pagination:
      #---- Pattern 1
      # first: "&laquo; 最初"
      # last: "最後 &raquo;"
      # next: "次 &rsaquo;"
      # previous: "&lsaquo; 前"
      # truncate: "&hellip;"
      #
      #---- Pattern 2
      # first: 先頭
      # last: 最終
      # previous: 前ページ
      # next: 次ページ
      # truncate: "..."
      #
      #---- Pattern 3
      first:    "&laquo;"
      last:     "&raquo;"
      previous: "&lsaquo;"
      next:     "&rsaquo;"
      truncate: "&hellip;"
